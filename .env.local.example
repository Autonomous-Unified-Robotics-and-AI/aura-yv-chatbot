# Database URLs (Vercel will automatically populate these in production)
PRISMA_DATABASE_URL="postgresql://username:password@localhost:5432/yv_chatbot_db?connection_limit=20&pool_timeout=20"
POSTGRES_URL="postgresql://username:password@localhost:5432/yv_chatbot_db"

# For local development, you can use a local PostgreSQL instance
# or get these from your Vercel project dashboard under Settings > Environment Variables

# API Configuration
OPENAI_API_KEY="your-openai-key"
ANTHROPIC_API_KEY="your-anthropic-key"
NOTION_INTEGRATION="your-notion-integration-token"

# Backend URL (for data extraction to post to)
FRONTEND_URL="http://localhost:3000"